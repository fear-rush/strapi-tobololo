'use strict';

/**
 * discover-tobololo service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::discover-tobololo.discover-tobololo');
